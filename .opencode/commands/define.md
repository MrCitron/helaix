---
description: Create or update one rigorous feature specification artifact.
agent: plan
---

Create or update the feature specification for `$ARGUMENTS`.

1. Read `AGENTS.md`, `docs/STYLEGUIDE.md`, `memory/constitution.md`, and any relevant existing specs or mockups.
2. Create or update exactly one file: `specs/[feature_name]/spec.md`.
3. Include these sections: Overview, Visual Requirements, Functional Requirements, Data Model, and Edge Cases.
4. Do not create a checklist, analysis file, plan, or implementation files during this command.
5. Keep the specification consistent with the Helix `.hlx` format requirements and the existing project architecture.
6. At the end, show the path and summarize the resulting `spec.md` artifact.
